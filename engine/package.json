{
  "name": "engine",
  "version": "1.0.0",
  "description": "Exchange engine",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "start": "node dist/index.js",
    "dev": "npm run build && npm run start",
    "dev:fresh": "rm -f snapshot.json && npm run build && npm run start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "redis": "^5.8.3",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "@types/node": "^24.7.0",
    "ts-node-dev": "^2.0.0"
  }
}